export enum ENUM_EMAIL_GATEWAY_ACCOUNT_TYPE {
  DEFAULT = 'default',
  CUSTOM = 'custom',
}

export enum ENUM_SMS_GATEWAY_ACCOUNT_TYPE {
  DEFAULT = 'default',
  CUSTOM = 'custom',
}

export enum ENUM_SMS_GATEWAY_REQUEST_METHOD {
  GET = 'GET',
  POST = 'POST',
}
