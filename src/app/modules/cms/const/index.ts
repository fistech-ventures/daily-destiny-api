export enum ENUM_LANGUAGE {
  ENGLISH = 'English',
  BENGALI = 'Bengali',
}

export enum ENUM_CONTENT_TYPE {
  STATIC = 'static',
  DYNAMIC = 'dynamic',
}

export enum ENUM_SECTION_TYPE {
  AD = 'ad',
  ARTICLE = 'article',
  POLL = 'poll',
  OPINION = 'opinion',
  MAGAZINE = 'magazine',
  PODCAST = 'podcast',
}

export enum ENUM_LAYOUT_TYPE {
  column = 'column',
  row = 'row',
  grid = 'grid',
}

export enum ENUM_SECTION_LAYOUT {
  SLIDER = 'slider',
  GRID = 'grid',
  LIST = 'list',
}
